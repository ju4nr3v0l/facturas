{% extends 'base.html.twig' %}
{% block content %}
{ #dump(clientes) #}
  <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
    <h1 class="page-header">Clientes</h1>
    <h2 class="sub-header">Edición Clientes</h2><h3>{{clientes.nombres}}</h3><h3>{{clientes.identificacion}}</h3>
    <div class="container fluid">
      {{ form_start(form) }}
	  {{ form_widget(form) }}
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}     